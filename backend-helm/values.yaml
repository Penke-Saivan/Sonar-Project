deployment:
  imageRepo: tonyduck/backend
  imageVersion: v2
  replicas: 3
service:
  servicePort: 5000
  targetPort: 5000
  containerPort: 5000

   # default value {{ .Values.service.servicePort }}
   #{{ .Values.service.targetPort }}

# {{ .Values.deployment.imageRepo }}
deployment:
  imageRepo: tonyduck/backend-deploy
  imageVersion: v1
  replicas: 1
service:
  servicePort: 5000
  targetPort: 5000
  containerPort: 5000

   # default value {{ .Values.service.servicePort }}
   #{{ .Values.service.targetPort }}

# {{ .Values.deployment.imageRepo }}
deployment:
  imageRepo: mysql
  imageVersion: 8
  replicas: 1
service:
  servicePort: 3306
  targetPort: 3306
  containerPort: 3306

   # default value {{ .Values.service.servicePort }}
   #{{ .Values.service.targetPort }}

# {{ .Values.deployment.imageRepo }}